<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simplechat">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleChat">
        <activity android:name="com.example.simplechat.splashscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.simplechat.MainActivity" />
        <activity android:name="com.example.simplechat.otpAuthentication" />
        <activity android:name="com.example.simplechat.setProfile" />
        <activity android:name="com.example.simplechat.specificchat"/>
        <activity android:name="com.example.simplechat.UpdateProfile" />
        <activity android:name="com.example.simplechat.ProfileActivity" />
        <activity android:name="com.example.simplechat.chatActivity" />
    </application>
</manifest>